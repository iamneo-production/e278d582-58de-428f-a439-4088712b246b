
CREATE OR REPLACE PROCEDURE FM_COUNT
AS
FM_CNT NUMBER;
BEGIN
SELECT COUNT(*) INTO FM_CNT
FROM AIRLINE
WHERE GENDER='Female';
DBMS_OUTPUT.PUT_LINE('THE TOTAL COUNT OF FEMALE PASSENGER BOOKED : ' || FM_CNT);
END;
/

BEGIN
FM_COUNT();
END;
/
